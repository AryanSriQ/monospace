# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build monospace-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t monospace-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system monospace-api && \
          adduser --system -G monospace-api monospace-api

COPY dist/apps/monospace-api monospace-api
RUN chown -R monospace-api:monospace-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix monospace-api --omit=dev -f install

CMD [ "node", "monospace-api" ]
